
<form action="/pizzas" method="GET">
<div class="container">
        <div class="row">
            <div class="col-lg-12 text-left">
                <h1 class="mt-5 text-center" >Search for Food !</h1>
            </div>
        </div>
<label>Name</label>
<input text="text" name="name" value="<%= searchOptions.name %>">
<button type="search">Search</button>
</form>
<br>
 <div class="text-right"> 
<a class='btn btn-success 'href="/pizzas/new">Add New Food</a></td>
</div><br>
 <table id="menuTable" class="indent">
                    <thead>
                        <tr>
                            <th colspan="4">Italian Restaurant Menu</th>
                             <tr>
                            <th>Item</th>
                            <th>View</th>
                            <th>Edit</th>
                            <th>Delete</th>
                        </tr>
                        </tr>
                        <tr>
                        </thead>
                    <tbody>

                          
    <% pizza.forEach(pizza =>{ %>
    <div>
        <td><%= pizza.name %></th>
        <td><a class="btn btn-primary" href="/pizzas/<%= pizza.id %>">view</a></td>
        <td><a class="btn btn-secondary" href="/pizzas/<%= pizza.id %>/edit">Edit</a></td>
        
        <td><%- include('../partials/delete_form',
              { url: `/pizzas/${pizza.id}` }) %></td>
              <tr>
    
<%})%>



